# Screenshots

- `page.png`: the page fetching three saved employees from local MongoDB.
- `sample-page.png` and `sample-error.png`: earlier checks before MongoDB setup.

The MongoDB API returned HTTP 200 and three documents with MongoDB ObjectIds.

Still needed for submission: browser Console (`console.png`), Network tab with status 200 (`network.png`), and the final stopped-backend page (`error.png`). Embed these in the project README after capturing them.
